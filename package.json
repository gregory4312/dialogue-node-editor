{
  "name": "@workspace/root",
  "version": "0.0.0",
  "private": true,
  "author": "Aevarkan",
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "pnpm -F @workspace/common run build && pnpm -F @workspace/app run build && pnpm -F bedrock-visual-dialogue-editor run build",
    "build:quick": "pnpm -F @workspace/app run build-only && pnpm -F bedrock-visual-dialogue-editor run build",
    "build:dev": "pnpm -F @workspace/app run build:dev && pnpm -F bedrock-visual-dialogue-editor run build",
    "package": "pnpm -F bedrock-visual-dialogue-editor run package",
    "dev:webapp": "pnpm --filter @workspace/app dev"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/vscode": "^1.105.0",
    "eslint": "^9.39.1",
    "fs-extra": "^11.3.2",
    "tslib": "^2.8.1",
    "typescript": "~5.9.3",
    "vitest": "^4.0.8"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "fast-deep-equal": "^3.1.3"
  }
}
